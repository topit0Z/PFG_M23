#!/bin/bash
# DTE-Admin-uninstall
BIN_DIR=/usr/bin
LIB_DIR=/var/lib

#Cargamos las variables
source $LIB_DIR/DTE-Admin-globals

#Cargamos las funciones
source $BIN_DIR/DTE-Admin-funciones


#Limpiamos el directorio de configuración
rm -rf $CONF_DIR/*

restore_backups
